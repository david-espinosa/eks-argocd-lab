config:
  outputs: |
    [OUTPUT]
        Name es
        Match *
        Host elasticsearch-master
        Port 9200
        HTTP_User elastic
        HTTP_Passwd rsYVcDuxlOFXgVJh
        tls On
        tls.verify Off
        Logstash_Format On
        Logstash_Prefix fluent-bit
        Replace_Dots On
        Retry_Limit False
        Suppress_Type_Name On